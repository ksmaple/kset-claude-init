# 文件与目录规范

## 临时文件与文档生成
- 临时文档、脚本、辅助生成物等**非核心代码文件**必须存放在 `tmp/` 子目录下
- `tmp/docs/` — 临时文档、分析结果、生成报告
- `tmp/scripts/` — 一次性脚本、辅助工具脚本
- `tmp/` 下其他子目录可按需创建，如 `tmp/data/`、`tmp/notes/` 等
- **禁止**将临时文件直接放在项目根目录或源码目录中
- 任务结束后，若临时文件不再需要，应主动清理
